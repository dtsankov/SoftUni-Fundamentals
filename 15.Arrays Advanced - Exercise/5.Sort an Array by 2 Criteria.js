function sortingArray(arr){
   

  console.log(arr = arr.sort((a, b) => a.length - b.length || a.localeCompare(b)).join('\n'))

 
}
sortingArray(['alpha', 'beta', 'gamma'])
sortingArray(['Isacc', 'Theodor', 'Jack', 'Harrison', 'George','Anabel'])